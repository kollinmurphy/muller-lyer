export const DIAGRAM_WIDTH = 800;
export const DIAGRAM_HEIGHT = 200;

export const OBLIQUE_LENGTH = 10;
export const OBLIQUE_X = Math.round(Math.sin(36 * (Math.PI / 180)) * OBLIQUE_LENGTH);
export const OBLIQUE_Y = Math.round(Math.cos(36 * (Math.PI / 180)) * OBLIQUE_LENGTH);

export const LINES_VERTICAL_OFFSET = 75;
export const LINES_HORIZONTAL_OFFSET = 50;

export const MOMENTARY_EXPOSURE_MS = 50;
export const PROLONGED_EXPOSURE_MS = 1000;
export const RESPONSE_DELAY_MS = 5000;
